server:
  port: 8001

spring:
  application:
    name: cloud-provider-hystrix-payemnt

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      #单机版eureka配置
      defaultZone: http://eureka7001.com:7001/eureka
